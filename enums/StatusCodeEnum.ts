export enum StatusCodeEnum {
    BAD_REQUEST = 400,
    CREATED = 201,
    FORBIDDEN = 403,
    INSUFFICIENT_STORAGE = 507,
    NOT_FOUND = 404,
    OK = 200,
    PAYLOAD_TOO_LARGE = 413,
    REQUEST_TIMEOUT = 408,
    SERVER_ERROR = 500,
    TOO_MANY_REQUESTS = 429,
    UNAUTHORIZED = 401
}
